import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faEnvelope, faSignOutAlt, faKey } from "@fortawesome/free-solid-svg-icons";

export const logoutIcon = <FontAwesomeIcon icon={faSignOutAlt} />;

export const emailIcon = <FontAwesomeIcon icon={faEnvelope} />;

export const passwordIcon = <FontAwesomeIcon icon={faKey} />;
